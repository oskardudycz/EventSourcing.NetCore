{
    "Logging": {
        "LogLevel": {
            "Default": "Warning"
        }
    },
    "AllowedHosts": "*",

    "elasticsearch": {
        "index": "meetings",
        "url": "http://localhost:9200/"
    },

    "KafkaConsumer": {
        "ConsumerGroup": "MeetingsSearch",
        "Endpoint": "localhost:9092",
        "Topics": "MeetingsManagement"
    },

    "KafkaProducer": {
        "Endpoint": "localhost:9092",
        "Topic": "MeetingsSearch"
    }
}
